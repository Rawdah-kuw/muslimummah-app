// Minimal placeholder test so `flutter create` does not inject the default
// counter-app template test (which would not compile against this app).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('app package loads', () {
    expect(1 + 1, 2);
  });
}
